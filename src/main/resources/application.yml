spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/aureus
    username: aureus
    password: aureus
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
  security:
    user:
      name: disabled
      password: disabled

server:
  port: 8080

security:
  jwt:
    secret: ${JWT_SECRET:h/YBgt/QlM57SVbqiLFb2CRATkMjUnoNyDNvudfr24s=}
    expiration: 86400000

openai:
  api-key: ${OPENAI_API_KEY:placeholder}
  model: gpt-4.1-mini